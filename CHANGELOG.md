# 版本记录

每个版本回答三件事：发现了什么问题、改了什么、怎么验证。
迭代类型如实标注。本项目至今**没有真实企业用户或结算从业者反馈**（访谈计划见 `docs/ai-pm/USER_RESEARCH_PLAN.md`，尚未执行），以下全部属于工程迭代与规则／评测迭代，不是用户驱动迭代。所有指标来自注入真值的模拟数据，不代表生产环境准确率。

## v0.3.2 — 2026-09-24 · 重跑逐字节一致与文档对齐

**迭代类型**：工程迭代（可复现性）＋文档修正

**发现的问题**
- 幽灵单的 order_id 用 `uuid4()` 生成，不受 `RNG_SEED` 控制。每次重跑 ID 都不同，按 ID 划分的 20% 留出集行数随之漂移（三次复跑得到 19,830／19,853／19,848 条）。
- 修这个问题时发现，行序也不固定：异常清单和应收排行只按金额排序，金额并列的行每次顺序不同；`audit_agent` 编号前又用不稳定排序重排了一次。结果是同一个 `case_id` 重跑后可能指向另一张订单，而人工决定正是以 `case_id` 为键保存的。评估明细导出前也没有排序。
- 文档与代码不一致：`PROMPT_SPEC` 仍写 Prompt v2，代码是 v3（v0.3 起新增 4 个合同类证据 ID）；`CONTROLLED_WORKFLOW` 写人工决定存进 CSV，实际是 SQLite；`generate_data.py` 头注释仍把系统运费当对账真值，并称金额"可视作元"（实为巴西雷亚尔）。

**改动**
- 幽灵单 ID 改由种子与序号哈希得出（`_ghost_order_id`），不消耗全局随机数，其他随机抽样与修复前逐位一致；另加 ID 与真实订单不冲突的校验和一条单元测试。
- `recon_exceptions`、`recon_summary`、`bad_debt_candidates`、`ar_by_client` 的 `ORDER BY` 补主键作并列排序键；`audit_agent` 改用「金额降序＋order_id＋seller_id」稳定排序后编 `case_id`；评估明细按主键排序后导出。
- `PROMPT_SPEC` 更新到 v3（11 个证据 ID、`output_contract`、版本记录）；`CONTROLLED_WORKFLOW` 按代码重写审计字段、SQLite 存储、角色权限与 `case_id` 编号规则；`generate_data.py` 头注释改为合同费率基准与 BRL 口径。

**验证**（2026-09-24 在干净克隆上复跑）
- 同一输入连跑三次：`data/generated/` 下 6 个文件与 `output/` 下 9 个产物（8 个 CSV＋复核备忘录）三次逐字节一致；`run_manifest.json` 与 SQLite 库含运行时间戳，不在比较范围内。
- 与修复前对比：只有 1,200 个幽灵单的 order_id 变了。对账汇总、应收 KPI、账龄分布与全量指标完全相同（99,418 条，TP 13,318／FP 6／FN 0）。
- 20% 留出集固定为 19,852 条，查准率与召回率均为 100%。
- 22 项自动化测试全部通过；离线评估 21 条金标、10 条安全兜底、4 条护栏全部通过。

## v0.3.1 — 2026-09-24 · 签收时间缺失值修复

**迭代类型**：规则迭代（缺陷修复）

**发现的问题**
复核建议层用 `bool(delivered_at)` 判断有没有客户签收时间。DuckDB 把缺失的签收时间读成 `pd.NaT`，而 `bool(pd.NaT)` 为真，于是 407 条 `NOT_DELIVERED` 案件全部被当成"与签收时间冲突"转人工，真正有冲突的只有 9 条。`NOT_BILLED` 的已送达检查有同样的问题。金标案例传的是 `None`，所以测试没有发现。另外，README 顶图还停留在 v0.1 的数字（查全率 99.9%、"¥139,511"，并把雷亚尔标成了人民币）。

**改动**
- 新增 `_has_timestamp()`：`None`、`NaN`、`NaT` 与空字符串一律视为缺失，两处判断改用它。
- 新增 3 条回归测试（`tests/test_audit_review.py`），直接用 `pd.NaT` 构造证据。
- README 顶图 `docs/demo.svg` 换成 v0.3 实跑数字，删去可追回金额（本项目不对外声称可追回金额）。
- 分析看板 `docs/settlement_dashboard.html` 同步换成 v0.3.1 实跑数字：KPI 改为自动放行／转人工／自动执行，引擎验证补查准率，金额单位统一标为巴西雷亚尔，方法论改为合同费率基准。

**验证**（2026-09-24 在干净克隆上复跑）
- 21 项自动化测试全部通过；新增 3 条中有 2 条在修复前失败。
- 离线评估：21 条金标、10 条安全兜底、4 条模型输出护栏全部通过。
- `NOT_DELIVERED` 407 条：398 条给出拒付建议，全部为真未送达；9 条证据冲突（含全部 6 条误报）转人工复核。
- 对账层指标不变：99,418 条召回率 100%、查准率 99.955%（TP 13,318／FP 6／FN 0）。
- 13,324 条异常仍全部须人工审批，自动执行 0 条。

## v0.3 — 2026-07-20 · 合同费率基准与可审计证据

**迭代类型**：规则迭代 + 工程迭代

**发现的问题**
此前的三方对账把"系统应计运费"当作真值，只比对账单与系统是否一致。如果系统应计本身算错，对账发现不了。应付金额应该由合同决定，而不是由系统记录决定。

**改动**
- 引入 72 条版本化承运商费率（8 家承运商 × 3 服务区 × 3 重量带），先算逐单合同预期金额，再与履约证据和账单比对；金额判定只依据合同预期。
- 案件与证据包同时给出费率卡版本、合同条款号、服务区、计费重量、合同预期金额与原始运费。
- 证据包接口：导出源记录、规则结论、人工决定、事件时间线、规则及输入文件哈希。
- 人工决定与事件改由 SQLite 事务存储（`src/operational_store.py`），带 `schema_migrations` 版本迁移。
- 评估改为全量集 + 确定性 20% 留出集，输出 Precision、Recall、F1、误报率及分类别指标。
- 每次完整流水线生成 `output/run_manifest.json`，记录 run_id、策略版本、输入／规则 SHA-256、表行数与评估结果。
- 发布可交互的异常复核沙盒与公开演示页。

**验证**（2026-09-19 在干净工作树复跑，run `RECON-20260919T125152Z`）
- 全量 99,418 条评估记录：召回率 100%，查准率 99.955%，误报率 0.007%（TP 13,318／FP 6／FN 0）。
- 确定性 20% 留出集 19,831 条：查准率与召回率均为 100%。
- 分类别最弱项：`NOT_DELIVERED` 查准率 98.28%，对应"未送达判定与签收状态冲突"（状态同步延迟），处置为转人工复核，已列入 Bad Case 清单。
- 18 项自动化测试全部通过。

## v0.2 — 2026-07-17 · 受控复核工作流与评测

**迭代类型**：模型／规则评测迭代 + 工程迭代

**发现的问题**
v0.1 的"异常复核 Agent"直接给处置建议，没有约束：模型可能改写规则裁定、引用不存在的证据，人工决定也可以被后一次请求覆盖。

**改动**
- 先写 PRD（`docs/ai-pm/PRD.md`）：结算专员、结算主管、财务／风控三类角色，四条用户故事与验收标准；范围只到"建议与审批"，不执行任何资金动作。
- 案件结论一律由确定性规则给出；大模型只在显式配置密钥后生成受约束摘要，输出须通过 Schema、证据引用和规则一致性校验，否则回退到规则理由。
- 人工决定 `PENDING → APPROVED / REJECTED / ESCALATED` 只允许转移一次；相同幂等键可安全重放，不同请求不能覆盖首条决定。
- 注入的异常标签只在规则执行完成后用于离线评估，不进入 SQL 判定或建议链路。

**验证**
- 21 条非重复金标案例、10 条证据缺失或冲突的安全案例、4 条模型护栏场景，2026-09-19 复跑全部通过。
- 修复跨平台测试发现问题，测试可在 Windows 与 Linux CI 上一致运行。

## v0.1.1 — 2026-07 · 文档清理

删除 README 中与项目无关的求职文案（PR #2）。仓库只放作品本体。

## v0.1 — 2026-07-01 · 首版

Olist 公开电商数据 + 合成承运商账单；DuckDB SQL 三方对账（订单 × 承运商账单 × 容差）、异常分类与金额影响排序；应收五档账龄、DSO 与 ECL；展示级 HTML 分析报告。

## 尚未做的事

- **真实用户研究**：`USER_RESEARCH_PLAN.md` 计划访谈 5–8 位一线结算人员，至今未执行。PRD 中的角色与痛点是基于结算流程的产品假设，未经验证。
- **北极星指标无数据**：PRD 定义的北极星是"人工复核后被采纳的高置信度规则建议占比"，事件埋点已就位，但没有真实复核人使用，因此该指标目前没有任何读数。
- 金额为 Olist 原始币种（巴西雷亚尔），项目不对"可追回金额"做折算或对外声称。
